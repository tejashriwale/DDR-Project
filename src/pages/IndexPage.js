import React from 'react';

const IndexPage = () => {
  return (
    <div>
      <h1>Index Page</h1>
      <button onClick={() => alert('Button clicked on Index Page')}>Index Button</button>
    </div>
  );
};

export default IndexPage;
